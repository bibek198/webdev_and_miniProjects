/*
String
Number
Boolean
Bigint

Undefined
null -> empty : null is different from undefined, 
null is empty and empty does not mean 0

Object

Symbol

*/

var score = 102; // not used now, its a old way and have some problems
let newScore = 102;
let isBoolean = false;
let isTrue = true;

// object
let teaTypes = ["lemon tea", "orange tea", "oolong tea"]; // this object is Array
let user = {firstname: "Bibek", lastname: "Chaudhary"};

// copy from another variable
let getScore = newScore;
console.log(getScore);
